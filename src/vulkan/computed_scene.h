#pragma once

#include "common_objects.h"

namespace VulkanImpl {

class ComputedScene {
public:
    ComputedScene(const GraphicalEnvironmentSettings& settings) {}


private:
    const GraphicalEnvironmentSettings _settings;
};

} // namespace
